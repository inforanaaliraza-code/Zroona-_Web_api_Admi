# npmrc configuration for paths with special characters
# This helps npm handle paths containing & and other special characters on Windows

# Use PowerShell for scripts (handles paths better than cmd.exe)
# Commented out for macOS compatibility - npm will use default shell
# script-shell=powershell.exe

# Disable optional dependencies that might cause issues
optional=false

# Legacy peer deps (already using --legacy-peer-deps flag)
legacy-peer-deps=true

