# Copy to .env.local and fill in values.

# API
NEXT_PUBLIC_API_BASE_URL=http://localhost:3434/api/
NEXT_PUBLIC_API_URL=http://localhost:3434/api

# Google Maps API (required for event location map)
# Get key: Google Cloud Console → APIs & Services → Credentials → Create API key
# Enable: Maps JavaScript API, Places API, Geocoding API
# Key restrictions → Application restrictions: Websites
# Add these Website restrictions (HTTP referrers):
#   http://localhost:3000/*
#   http://127.0.0.1:3000/*
#   https://zuroona.sa/*
#   https://www.zuroona.sa/*
#   https://*.zuroona.sa/*
# API restrictions: Restrict key → select the 3 APIs above. Save. Wait ~5 min.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
