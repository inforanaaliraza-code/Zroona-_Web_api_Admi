# ===========================================
# Production / VPS – copy to .env on server
# Kab: VPS pe pehli baar deploy se pehle
# Kahan: /var/www/Zuroona/.env
# ===========================================

# Ports (optional; defaults in docker-compose)
API_PORT=3434
WEB_PORT=3000
ADMIN_PORT=3002

# ----- API / Backend -----
MONGODB_URI=mongodb://mongodb:27017/jeena
SECRET_KEY=your-production-secret-key-min-32-chars
# CORS (production mein false rakhna better)
CORS_ALLOW_ALL=false

# AWS S3 (uploads)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_BUCKET_NAME=
AWS_REGION=

# URLs (production domains – zaroor set karein)
# API ye use karta hai CORS / redirects ke liye
FRONTEND_URL=https://zuroona.sa
ADMIN_URL=https://admin.zuroona.sa
WEB_URL=https://zuroona.sa

# Optional: PKEYPATH, CPATH (agar API mein use ho rahe hon)

# ----- Web (Next.js) – build time / runtime -----
NEXT_PUBLIC_API_BASE_URL=https://api.zuroona.sa/api/
NEXT_PUBLIC_API_URL=https://api.zuroona.sa/api
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
NEXT_PUBLIC_S3_BUCKET=
NEXT_PUBLIC_S3_REGION=
NEXT_PUBLIC_S3_URL=
NEXT_PUBLIC_S3_DIR=Zuroona

# ----- Admin (Next.js) -----
NEXT_PUBLIC_API_BASE_URL=https://api.zuroona.sa/api/
NEXT_PUBLIC_API_ADMIN_BASE_URL=https://api.zuroona.sa/api/admin/
NEXT_PUBLIC_MAPS_API_KEY=
NEXT_PUBLIC_S3_BUCKET=
NEXT_PUBLIC_S3_REGION=
NEXT_PUBLIC_S3_URL=
NEXT_PUBLIC_S3_DIR=Zuroona

# ----- API extra (mail, payments, etc – api/.env se copy karein) -----
# SMTP_HOST= SMTP_PORT= SMTP_USER= SMTP_PASS=
# CLOUDINARY_*=  SENTRY_DSN=  MOYASAR_*=  etc.
